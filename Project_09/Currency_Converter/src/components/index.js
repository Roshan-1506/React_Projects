// In big projects where the components are more, this approach is mostly followed where we make one seperate file and in that we import and export all the files of the project.

import InputBox from "./InputBox"

export {InputBox}